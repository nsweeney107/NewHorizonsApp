<Page
    x:Class="NewHorizonApp.Views.Cookies"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NewHorizonApp.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="DarkGray">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <MediaElement x:Name="mediaPlayer"
                    Source="/Assets/cookies.mp4"
                    AutoPlay="True"
                    AreTransportControlsEnabled="True"/>
        <Button Style="{StaticResource StandardStylizedButtonStyle}"
                Grid.Row="1"
                VerticalAlignment="Center"
                HorizontalAlignment="Center"
                Click="HomeClick">
            <Button.Content>
                <TextBlock  Style="{StaticResource ButtonTextBlockStyle}"
                            Text="Return to Home Page"/>
            </Button.Content>
        </Button>
    </Grid>
</Page>
